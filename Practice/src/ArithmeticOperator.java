
public class ArithmeticOperator {
	int sum(int a, int b) 
	{
		return a+b; 
	}
	int sub(int m, int n) //Observe that the variable names are changed
	{
		return m-n; 
	}
	int mul(int p, int q) //Observe that the variable names are changed
	{
		return p*q; 
	}
	int div(int x, int y) //Observe that the variable names are changed
	{
		return x/y; 
	}
	int mod(int x, int y) //Observe that the variable names are changed
	{
		return x%y; 
	}

}
